<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fun.liwudi.authservice.mapper.PublicVerifyMapper">

    <select id="findOne" parameterType="fun.liwudi.authservice.bean.PublicVerifyDto" resultType="fun.liwudi.authservice.bean.PublicVerifyDto">
        select id, create_time, create_user, update_time,
               update_user, delete_flag, ak_name, ak_pass
        from auth_ak
        <where>
            delete_flag = 0
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="createUser != null and createUser != ''">
                and create_user = #{createUser}
            </if>
            <if test="updateTime != null and updateTime != ''">
                and update_time = #{updateTime}
            </if>
            <if test="updateUser != null and updateUser != ''">
                and update_user = #{updateUser}
            </if>
            <if test="createTime != null and createTime != ''">
                and create_time = #{createTime}
            </if>
            <if test="name != null and name != ''">
                and ak_name = #{name}
            </if>
            <if test="pass != null and pass != ''">
                and ak_pass = #{pass}
            </if>
        </where>
    </select>

</mapper>